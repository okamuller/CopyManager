# CopyManager 仕様書

**バージョン**: 1.0.0
**作成日**: 2026-03-21
**対象プラットフォーム**: Windows 10 / 11 (x64)

---

## 1. 目的・背景

### 1.1 目的
複数の大容量ファイル・フォルダを大容量 NAS へコピーする際に、
FastCopy をバックエンドとして活用しながらジョブを一元管理するツールを提供する。

### 1.2 解決する課題
- 大容量ファイルの転送中は他の作業ができないため、コピーをキューに積んで順次実行したい
- 複数のコピー先・コピー元が混在するため、ジョブごとに設定を保存・再利用したい
- コピーの進捗をリアルタイムで一覧確認したい
- ネットワーク不安定な環境での転送失敗を自動リトライしたい

### 1.3 前提条件
- FastCopy (3.x 以降) が対象 PC にインストールされていること
- コピー先 NAS は Windows からドライブレターまたは UNC パス (`\\NAS\share\`) でアクセス可能であること

---

## 2. ユーザーストーリー

| ID | ストーリー |
|----|-----------|
| US-01 | コピージョブを追加して、キューに並べて順番に実行したい |
| US-02 | 実行中のジョブの進捗率・転送速度・残り時間をリアルタイムで確認したい |
| US-03 | ジョブが失敗した場合は自動でリトライしてほしい。手動でもリトライできる |
| US-04 | 完了・失敗・キャンセル済みのジョブ履歴を確認したい |
| US-05 | アプリを再起動しても未完了のジョブが残っていてほしい |
| US-06 | FastCopy のパスやバッファサイズなどのオプションを設定できるようにしたい |
| US-07 | ジョブを並び替えたり、不要なジョブを削除したい |

---

## 3. 機能要件

### 3.1 ジョブ管理

#### 3.1.1 ジョブの作成
- ソースパス (ファイル or フォルダ) を複数指定できる
- 宛先パス (フォルダ) を1つ指定する
- ジョブ名を任意で設定できる (省略時は自動生成)
- コピーモードを選択できる: 差分コピー / 全上書き / 移動
- FastCopy オプションを個別に上書き設定できる (バッファサイズ・速度等)

#### 3.1.2 ジョブのキュー管理
- ジョブはキューに追加され、先入れ先出し (FIFO) で実行される
- ジョブの順序をドラッグ＆ドロップで変更できる
- 実行前のジョブを編集・削除できる
- ジョブを一時停止 (FastCopy プロセスの一時停止) できる
- ジョブをキャンセルできる

#### 3.1.3 ジョブの状態遷移

```
[追加]
  │
  ▼
Pending (待機中)
  │  キューの先頭に達し、実行スロットが空いた
  ▼
Running (実行中)
  │            │              │
  ▼            ▼              ▼
Completed    Failed         Cancelled
(完了)      (失敗)         (キャンセル)
              │
              ▼ 自動リトライ (上限未満)
            Pending (再キューイング)
```

### 3.2 進捗モニタリング

- ジョブ一覧に各ジョブの進捗率を表示する (ProgressBar)
- 実行中ジョブの転送速度 (MB/s)、転送済みバイト数、残り時間を表示する
- FastCopy の標準出力をリアルタイムでパースして取得する
- ジョブ完了時に合計転送量・所要時間を記録する

### 3.3 エラー処理・リトライ

- FastCopy の終了コードが非 0 の場合、ジョブを `Failed` に遷移する
- リトライ間隔・最大リトライ回数は設定で変更可能
  - デフォルト: リトライ間隔 30 秒、最大 3 回
- リトライ回数を超えた場合: `Failed` 状態で停止し、ユーザーに通知する
- 手動リトライ: `Failed` 状態のジョブをキューの末尾に再追加できる
- エラーログ (FastCopy 出力) をジョブに紐付けて保存・表示する

### 3.4 設定

| 設定項目 | デフォルト値 | 説明 |
|---------|-------------|------|
| FastCopy.exe パス | 自動検出 | FastCopy.exe の絶対パス |
| バッファサイズ | 256 MB | FastCopy /bufsize |
| 転送速度 | full | FastCopy /speed |
| 同時実行ジョブ数 | 1 | 最大並列実行数 |
| 最大リトライ回数 | 3 | 失敗時の自動リトライ上限 |
| リトライ間隔 (秒) | 30 | リトライまでの待機時間 |
| ログ保存先 | %APPDATA%\CopyManager\logs | FastCopy ログの保存ディレクトリ |
| ジョブ完了通知 | 有効 | Windows トースト通知 |

---

## 4. 非機能要件

| 項目 | 要件 |
|------|------|
| 対応 OS | Windows 10 (21H2 以降) / Windows 11 |
| アーキテクチャ | x64 |
| .NET バージョン | .NET 8 以降 |
| UI 応答性 | 転送処理中も UI がフリーズしないこと (非同期処理) |
| 起動時間 | 3 秒以内 |
| ジョブ永続化 | アプリ終了・クラッシュ後も未完了ジョブが復元できること |
| ログ保存期間 | 30 日間 (自動ローテーション) |
| インストーラー | xcopy デプロイ可能 (レジストリ不要) または MSIX/インストーラー |

---

## 5. UI 設計

### 5.1 メイン画面

```
┌─────────────────────────────────────────────────────────────────┐
│  CopyManager                                    [_][□][×]       │
├─────────────────────────────────────────────────────────────────┤
│ [+ ジョブ追加]  [▶ 全開始]  [⏸ 全一時停止]  [⚙ 設定]          │
├──────┬─────────────────────┬──────┬────────────┬───────┬────────┤
│ #    │ ジョブ名            │ 状態 │ 進捗       │ 速度  │ 操作   │
├──────┼─────────────────────┼──────┼────────────┼───────┼────────┤
│  1   │ 動画アーカイブ      │ 実行中│ ██████░░ 75%│ 85 MB/s│ [⏸][✕]│
│  2   │ 写真バックアップ    │ 待機中│ ─          │ ─    │ [▶][✕]│
│  3   │ ドキュメント整理    │ 完了  │ ██████████ │ ─    │ [🔄]   │
│  4   │ RAW データ転送      │ 失敗  │ ███░░░░░ 30%│ ─    │ [🔄][✕]│
├──────┴─────────────────────┴──────┴────────────┴───────┴────────┤
│ ステータスバー: 実行中 1件 / 待機中 1件 / 完了 1件 / 失敗 1件   │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 ジョブ追加・編集ダイアログ

```
┌────────────────────────────────────────────┐
│  ジョブの追加                     [×]      │
├────────────────────────────────────────────┤
│ ジョブ名: [動画アーカイブ_______]          │
│                                            │
│ コピーモード: (●) 差分コピー               │
│              ( ) 全上書き                  │
│              ( ) 移動                      │
│                                            │
│ コピー元:                                  │
│  [D:\Videos\2025\           ] [参照][削除] │
│  [E:\Backup\Photos\         ] [参照][削除] │
│  [+ コピー元を追加]                        │
│                                            │
│ コピー先:                                  │
│  [\\NAS01\archive\2025\     ] [参照]       │
│                                            │
│ ▼ 詳細オプション                           │
│   バッファサイズ: [256] MB                 │
│   転送速度: [full ▼]                       │
│                                            │
│        [キャンセル]    [追加]              │
└────────────────────────────────────────────┘
```

### 5.3 ジョブ詳細パネル (行クリックで展開)

```
┌─────────────────────────────────────────────┐
│ ジョブ詳細: 動画アーカイブ                  │
│ コピー元: D:\Videos\2025\                   │
│ コピー先: \\NAS01\archive\2025\             │
│ 開始時刻: 2026-03-21 14:32:10               │
│ 転送済み: 42.3 GB / 56.7 GB                 │
│ 経過時間: 00:08:32  残り: 00:02:41          │
│ リトライ: 0 / 3                             │
│                                             │
│ [FastCopy ログを表示]                       │
└─────────────────────────────────────────────┘
```

---

## 6. データモデル

### 6.1 CopyJob

```csharp
public class CopyJob
{
    public Guid Id { get; set; }
    public string Name { get; set; }
    public List<string> SourcePaths { get; set; }
    public string DestinationPath { get; set; }
    public CopyMode Mode { get; set; }          // Diff, ForceCopy, Move
    public JobStatus Status { get; set; }       // Pending, Running, Completed, Failed, Cancelled
    public int Priority { get; set; }           // キュー内の順序
    public JobOptions Options { get; set; }     // FastCopy オプション上書き
    public int RetryCount { get; set; }
    public int MaxRetryCount { get; set; }
    public DateTime? StartedAt { get; set; }
    public DateTime? CompletedAt { get; set; }
    public long TransferredBytes { get; set; }
    public long TotalBytes { get; set; }
    public string? LastErrorMessage { get; set; }
    public string? LogFilePath { get; set; }
}

public class JobOptions
{
    public int? BufferSizeMb { get; set; }      // null = グローバル設定を使用
    public string? Speed { get; set; }
}

public enum CopyMode { Diff, ForceCopy, Move }

public enum JobStatus { Pending, Running, Paused, Completed, Failed, Cancelled }
```

### 6.2 AppSettings

```csharp
public class AppSettings
{
    public string FastCopyExePath { get; set; }
    public int DefaultBufferSizeMb { get; set; } = 256;
    public string DefaultSpeed { get; set; } = "full";
    public int MaxConcurrentJobs { get; set; } = 1;
    public int MaxRetryCount { get; set; } = 3;
    public int RetryIntervalSeconds { get; set; } = 30;
    public string LogDirectory { get; set; }
    public bool EnableToastNotification { get; set; } = true;
}
```

---

## 7. FastCopy 連携仕様

### 7.1 コマンドライン構築

```
FastCopy.exe
  /cmd=<diff|force_copy|move>
  /bufsize=<N>m
  /speed=<full|autoslow|1..9>
  /log /logfile="<logFilePath>"
  /auto_close
  /no_confirm
  /error_stop
  "source1" "source2" ...
  /to="destination\"
```

### 7.2 進捗のパース

FastCopy の標準出力から以下のパターンをパースする:

```
# 例: 進捗行
Total: 42,345,678,912 / 56,789,012,345 bytes  (74.6%)  85.3 MB/s  残 02:41

# 完了行
End: Total 56.7GB / 56.7GB (100%)  Elapsed: 00:11:13
```

| フィールド | 正規表現 (例) |
|-----------|--------------|
| 進捗率 (%) | `\((\d+\.\d+)%\)` |
| 転送速度 | `(\d+\.\d+) MB/s` |
| 残り時間 | `残\s*(\d+:\d+)` |
| 転送済み | `(\d[\d,]*) /` |
| 合計バイト | `/ (\d[\d,]*)` |

### 7.3 終了コード

| コード | 意味 |
|--------|------|
| 0 | 正常完了 |
| 非 0 | エラー (詳細はログを参照) |

---

## 8. ファイル・ディレクトリ構成 (実行環境)

```
%APPDATA%\CopyManager\
├── settings.json       # アプリ設定
├── jobs.json           # ジョブリスト (永続化)
└── logs\
    ├── app.log         # アプリケーションログ
    └── jobs\
        ├── <JobId>.log # FastCopy 実行ログ (ジョブごと)
        └── ...
```

---

## 9. 通知

- ジョブ完了時: Windows トースト通知 (設定で無効化可能)
  - 通知内容: ジョブ名・完了/失敗・転送量・所要時間
- リトライ発生時: ステータスバーに表示 (トースト通知は行わない)
- 全ジョブ完了時: トースト通知 + アプリアイコンを通常に戻す

---

## 10. 将来拡張 (スコープ外)

以下は現バージョンのスコープ外だが、設計時に考慮する:

- スケジュール実行 (指定日時に自動開始)
- コピー完了後の PC シャットダウン
- 複数 NAS 宛先への同時コピー (並列実行数の拡張)
- ジョブテンプレート機能
- コマンドライン引数によるジョブ追加・実行 (バッチ連携)
